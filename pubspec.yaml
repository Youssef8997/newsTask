name: newsy_test
description: "News app test"
version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  dio: ^5.3.0
  flutter_bloc: ^8.1.0
  get_it: ^7.6.0
  fpdart: ^0.4.0
  equatable: ^2.0.3
  simple_connection_checker: ^0.3.4
  cached_network_image: ^3.4.1
  webview_flutter: ^4.11.0

dev_dependencies:
  build_runner: ^2.4.6
  json_serializable: ^6.7.0
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true
  assets:
    - assets/logo.png